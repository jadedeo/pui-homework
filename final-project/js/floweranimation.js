

/*



// Example: Handle user's answer
function handleAnswer(userAnswer, correctAnswer) {
    const isCorrect = userAnswer === correctAnswer;
    animateFlower(isCorrect);

    if (isCorrect) {
        console.log("Correct! The flower grows!");
    } else {
        console.log("Incorrect! The flower shrinks.");
    }
}

// Example usage: Simulate answer submissions
document.addEventListener("click", () => {
    const correctAnswer = "apple"; // Example correct answer
    const userAnswer = Math.random() > 0.5 ? "apple" : "banana"; // Simulate user answer

    handleAnswer(userAnswer, correctAnswer);
});
*/
